<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Rubik:ital,wght@0,300..900;1,300..900&display=swap"
    rel="stylesheet">
    <style>
      /* Loader Spinner */
      .loader {
          border: 4px solid #f3f3f3; /* Light gray */
          border-top: 4px solid #3498db; /* Blue */
          border-radius: 50%;
          width: 40px;
          height: 40px;
          animation: spin 1s linear infinite;
      }
      
      @keyframes spin {
          0% {
              transform: rotate(0deg);
          }
          100% {
              transform: rotate(360deg);
          }
      }
      </style>
      
</head>

<body class="font-poppins">
  <div class="flex h-screen bg-gray-100">
    <!-- Sidebar -->
    <aside class="w-64 bg-blue-900 text-white flex flex-col justify-between">
      <div class="px-6 py-4">
        <h2 class="text-2xl font-bold">Candidate Dashboard</h2>
        <!-- Sidebar Links -->
        <nav class="mt-8 space-y-2">
          <a href="/candidate/d/dashboard"
            class="flex items-center p-2 text-sm font-semibold rounded-md hover:bg-blue-700">
            <i class="fa-solid fa-house mr-3"></i> Home
          </a>
          <a href="/candidate/d/profile"
            class="flex items-center p-2 text-sm font-semibold rounded-md hover:bg-blue-700">
            <i class="fas fa-user mr-3"></i> Profile
          </a>
          <a href="#" class="flex items-center p-2 text-sm font-semibold rounded-md hover:bg-blue-700">
            <i class="fas fa-briefcase mr-3"></i> Job Applications
          </a>
          <a href="#" class="flex items-center p-2 text-sm font-semibold rounded-md hover:bg-blue-700">
            <i class="fas fa-envelope mr-3"></i> Messages
          </a>
          <a href="#" class="flex items-center p-2 text-sm font-semibold rounded-md hover:bg-blue-700">
            <i class="fas fa-bell mr-3"></i> Notifications
          </a>
          <a href="#" class="flex items-center p-2 text-sm font-semibold rounded-md hover:bg-blue-700">
            <i class="fas fa-cog mr-3"></i> Settings
          </a>
        </nav>
      </div>
      <!-- Logout Button -->
      <div class="px-6 py-4">
        <a href="#" class="flex items-center p-2 text-sm font-semibold rounded-md hover:bg-blue-700">
          <i class="fas fa-sign-out-alt mr-3"></i> Logout
        </a>
      </div>
    </aside>

    <!-- Main Content -->
    <div class="flex-1 p-6 space-y-6 overflow-y-auto">
      <% if (section==='dashboard' ) { %>
        <%- include('../dashboard-partials/home') %>
          <% } else if (section==='profile' ) { %>
            <%- include('../dashboard-partials/profile') %>
              <% } %>

    </div>
  </div>

</body>

</html>